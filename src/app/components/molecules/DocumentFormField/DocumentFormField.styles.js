const styles = {
  box: (isActive) => ({
    borderWidth: '2px',
    borderColor: '#AE445A',
    borderRadius: '10px',
    backgroundColor: isActive ? '#AE445A' : '',
    cursor: 'pointer'
  }),
};

export default styles